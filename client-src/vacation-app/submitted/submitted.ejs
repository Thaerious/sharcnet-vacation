<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="/vacation-app/submitted/submitted.css">
    <script type="importmap">
        <% if (typeof widget.prebuild === "undefined" || !widget.prebuild) { %>
        <%- include(widget.lib_file) %>
        <% } %>
    </script>
</head>
<body>
    <script type="module" src="/vacation-app/submitted/Submitted.mjs"></script>
    <% if (typeof widget.prebuild === "undefined" || !widget.prebuild) { %>
    <%- include(widget.template_file) %>
    <% } %>
    <%- include('./body.ejs'); %>
</body>
</html>

